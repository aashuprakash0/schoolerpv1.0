'use client';
import {useEffect,useState} from 'react'; import {supabaseBrowser} from '../../lib/supabase';
export default function Notices(){const [rows,setRows]=useState<any[]>([]);useEffect(()=>{supabaseBrowser().from('notices').select('id,title,body,category,published_at,expiry_date').eq('published',true).order('published_at',{ascending:false}).then(({data})=>setRows(data||[]))},[]);return <main className="page"><div className="page-head"><div><h1>Notice Board</h1><p className="muted">Adarsh Avasiya School public notices.</p></div></div>{rows.map(r=><article className="notice" key={r.id}><h3>{r.title}</h3><div className="notice-meta">{r.category} · {r.published_at?new Date(r.published_at).toLocaleDateString('en-IN'):''}</div><p style={{whiteSpace:'pre-wrap'}}>{r.body}</p></article>)}{!rows.length&&<div className="empty">No published notices.</div>}<div className="footer">© 2026 Adarsh Avasiya School</div></main>}
